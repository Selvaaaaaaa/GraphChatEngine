# Next Step – Milestone 07: Chatbot Web UI Integration

## Objective
Connect the static web UI (`ui/index.html`, `ui/app.js`, `ui/style.css`) to the backend endpoints (`POST /ingest` and `POST /chat`).

## Features to Implement
1. File upload drag-and-drop widget for CSV files.
2. Interactive chat conversation window sending user input to `POST /chat`.
3. Display generated answers, execution timing, and response messages in the chat interface.
