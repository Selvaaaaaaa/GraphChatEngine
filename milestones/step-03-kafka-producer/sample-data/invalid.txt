This is a plain text file.
It is NOT a CSV file.
It has the .txt extension.

PURPOSE:
Used to test the HTTP 400 validation path in POST /ingest.
The API should reject this file with:
  "error": "Only CSV files are supported."

[Test Data]
Row 1 | Col 1 | Col 2
Row 2 | Col 1 | Col 2
